# RV09-C00 - Implementation Audit Round 07 (FINAL)

5 role-less full auditors (architecture + code quality) re-reviewed the DV09 implementation under an OPEN-ENDED audit standard after the Round-6 revision (dynamic overlay SSR coverage with family class-string assertions, symmetric `trimLeadingArtifact`, robust `assertScaleInCompletedShape`, truthful `SAMPLER_WINDOW_MS` comment). Result: **5/5 acceptable (FINAL)**. All high confidence, all organic=clean, zero blocking. Loop-exit condition met.

## Tally

| Auditor | Verdict    | Blocking | Concerns | Organic | Confidence |
| ------- | ---------- | -------- | -------- | ------- | ---------- |
| 1       | acceptable | 0        | 0        | clean   | high       |
| 2       | acceptable | 0        | 0        | clean   | high       |
| 3       | acceptable | 0        | 0        | clean   | high       |
| 4       | acceptable | 0        | 0        | clean   | high       |
| 5       | acceptable | 0        | 0        | clean   | high       |

## Verified-FIXED across the implementation-audit loop (blocker progression)

Each round's blocking defect, resolved in the next, traced to its current state:

- **Round 1.** Family B (list <-> thread) scale animation not delivered; the FAB atom unmounted on overlay routes before the destination GesturePageLayout could play its forward enter animation. Family C used instant unmount instead of the plan's 200ms CSS transition. Lexical `FAB` leak in two shared-primitive comments. The journal's `bun run lint` EXIT 0 claim was inaccurate. All fixed: `fabConfig` now resolves the source-list FAB on overlay and compose routes at resting fraction 0; the Family B forward and back trajectories are sampler-driven via `listForegroundFromThreadCover`; Family C keeps the atom mounted and eases via a `fab-transition` class gated by `transitionEnabled`; the leaked comments reworded; the lint-exit claim corrected.
- **Round 2.** Family B forward was approximated by a CSS transition instead of the sampler; the back-swipe was broken (scale pinned at 1 from the first drag frame). The Family B e2e specs were tautological (endpoint-only assertions passed despite the broken animation). Fixed: `familyNeedsSamplerDuringDrag` keeps the sampler armed across the Family B drag; the resting-target check requires sample 1 with `!pager.dragging`; the forward holdover is gated on `!pager.dragging`; chip-exit is restricted to genuine cross-tab contexts; a sampler-gap holdover bridges the source/destination track-bind swap; `transitionEnabled` gated to the compose family only; `SAMPLER_TIMEOUT_MS` raised to 2000. The e2e specs sample `getComputedStyle(fab).transform` across the real gesture and assert the trajectory shape.
- **Round 3.** The Family B back-swipe sampler died with the pre-navigation document, and the 900ms window was too short for the holdover plateau under dev-server contention, causing flakes on correct code. The forward specs could not distinguish a smooth ramp from a one-frame snap. Fixed: `page.exposeBinding('__pushFabSample')` plus `page.addInitScript` keeps the sampler buffer on the Page object and re-arms the rAF loop on every new document; `SAMPLER_WINDOW_MS = 1800` matches the layer cap minus 200ms slack; the intermediate-in-(0.3,0.7) assertion is added to all forward specs. The two shared-primitive `onDestroy` callbacks gain a `browser` guard; the active-gesture-track docstrings are reworded to drop the `FAB` token.
- **Round 4.** Compose-back transition snapped because `transitionEnabled` was gated to `fabConfig?.family === 'compose'`, so the class stripped at the same flush as the family swap on the destination route. The `trimTrailingNoise` backward scan did not strip a trailing post-settle spike; the Family B back first-sample assertion was brittle to the CDP synchronous-touchMove artifact; the messages-variant Family C back spec was missing. Fixed: a `familyCInFlight` latch holds `transitionEnabled` across the compose<->list family swap for `FAMILY_C_TRANSITION_WINDOW_MS = 280`; `trimTrailingNoise` is rewritten as a forward scan; the Family B back first-sample is `min(samples[0..2]) < 0.2`; a `/messages/new` -> `/messages/inbox` Family C back spec added.
- **Round 5.** A deep-link to an overlay or compose route SSRs the FAB atom at scale 1 until hydration, because the shorthand `style:transform` directive bound to a `$derived` serialized the derived's getter/setter function body into the inline style attribute. The existing "no flash" e2e specs read post-hydration state and missed the defect. Fixed: the atom binds `style:transform={`scale(${scale}) translateY(${translateY}px)`}` via the value form, matching Header's pattern. A preventive SSR-style describe block fetches each route's SSR HTML via a raw `request.get` and asserts the resolved transform and the absence of the `function(` serialization signature.
- **Round 6.** The preventive SSR spec omitted the overlay route on a false `403` rationale (a real seeded `/discussion/<id>/<slug>` returns HTTP 200 for the admin id-0 session); the Family B back leading-spike flaked under load; the Family C back `lastScale > 0.85` endpoint was timing-brittle. Fixed: the SSR describe block is restructured to assert family-specific class strings (overlay vs compose vs list vs error-scale-0); a dynamic overlay test resolves a real discussion id+slug from the homepage SSR; a messages error-page test documents the pre-existing `/messages/1` 500 and asserts the atom still renders a valid transform; `trimLeadingArtifact` mirrors `trimTrailingNoise`; `assertScaleInCompletedShape` (`maxScale > 0.9` shape plus monotonicity plus 0.5-crossing) replaces the brittle endpoint assertion.
- **Round 7.** All Round-1-through-6 defects verified resolved. No new defects found.

## Confirmed product-correctness (all 5, Round 7)

- **SSR serialization valid on all 6 route classes.** JS-disabled raw fetch confirms the FAB atom serializes a resolved `transform` and no `function(` leak on any route: `/` and `/messages/inbox` resolve to `transform: scale(1) translateY(0px)` with neither `pointer-events-none` nor `fab-transition` (list family); `/post/discussion` and `/messages/new` resolve to `scale(0) translateY(0px)` with `pointer-events-none` and `fab-transition` (compose family); a dynamically-resolved `/discussion/<id>/<slug>` resolves to `scale(0) translateY(0px)` with `pointer-events-none` and NO `fab-transition` (overlay family, empirically distinct from compose); `/messages/1` resolves to `scale(0) translateY(0px)` on the pre-existing 500 error page (error-scale-0).
- **Preventive SSR test catches the regression.** Reverting the atom to the shorthand-bound-to-`$derived` form produces the `function(` leak and 6/6 SSR assertions fail, including the overlay route. Restoring the value-binding form passes all 6. One reviewer noted the `function(` sub-assertion is stale against Svelte 5.56.3 where the shorthand form happens to serialize correctly; the regex assertion still meaningfully constrains the serialization and is retained, with a softened comment. Non-blocking.
- **Every transition family ramps through 0.5 mid-window.** Verified in both directions and across both list routes: Family A tab swipe, Family B thread forward-tap and back-swipe finger-driven, Family C compose forward and back for discussions and messages.
- **Test reliability holds under load.** The symmetric `trimLeadingArtifact` plus `trimTrailingNoise` eliminates the Family B back CDP leading-spike flake. `assertScaleInCompletedShape` (`maxScale > 0.9` shape, not brittle `lastScale`) holds under load. The dynamic overlay-id resolution resolves from the homepage SSR rather than hardcoding an id, so it tracks the seed. `e2e/fab.spec.ts` ran 3 to 4 times for 21/21 each, zero flakes.
- **Organic integration clean.** `git diff` on the shared primitives the plan enumerates shows zero DV09-introduced `fab` / `post` / `messages` / `discussions` tokens in any added line, including comments and docstrings. `scroll-chrome.svelte.ts` gains only the general `headerHeight` getter and interface field. `active-gesture-track.svelte.ts` is named for the gesture concept, not for any consumer.
- **Gates green.** `bun run check` 0 errors / 0 warnings across 1431 files. `bun test src/` 206 pass / 0 fail (43 FAB unit tests across `fab-scale.test.ts` and `fab-routes.test.ts`). `bun run lint` exits 1 only on the pre-existing `src/app.css` prettier nit (reproduces on clean master; DV09 does not touch `app.css`); eslint 0 errors; similarity-ts type-duplicates 0. The full e2e suite reports only the pre-existing `header-tabs-replay` failure, which reproduces on clean master and is outside the DV09 surface.

## Carried-to-future (non-blocking)

- **Messages Family B e2e.** A `/messages/inbox` -> `/messages/[id]` Family B trajectory spec is blocked by the pre-existing `/messages/[id]` 500 from `src/lib/stores/mobile-tabs.ts:38` (outside DV09). The Family B math is symmetric with the discussions path and unit-covered in `src/lib/utils/fab-scale.test.ts`. The minimal fix is to backfill `page.data.messages.conversations` for the admin id-0 session or to guard `mobile-tabs.ts:38` against the missing field.
- **Preventive SSR test `function(` sub-assertion.** Stale against Svelte 5.56.3 where the shorthand-bound-to-`$derived` form happens to serialize correctly. Retain the regex transform assertion (it still meaningfully constrains the serialization); soften the comment that ties the assertion to the function-body signature.
- **`firstOverlayDiscussionPath` slug regex.** The character class `[A-Za-z0-9%_-]+` is the surface to re-examine if a future seed renders discussion slugs with characters outside that set.
- **`header-tabs-replay` pre-existing master flake.** Reproduces on clean master at `a8693dd`. Outside the DV09 surface.

## Loop-exit

Implementation-audit loop exit: 5/5 acceptable (FINAL). DV09 C00 is implementation-complete: plan 5/5 PASS (5 rounds) plus implementation 5/5 acceptable (7 rounds). Ready for commit/merge.
